apiVersion: v1
kind: Service
metadata:
  name: ml-api-svc
  namespace: bd-prd
spec:
  selector:
    app: bd-prd-ml
  ports:
    - name: ml-api-port
      port: 5000
      protocol: TCP
      targetPort: ml-api-port
