apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scraper-runemetrics
  namespace: bd-prd
spec:
  endpoints:
    - path: /
      port: scraper-rm-metrics
  selector:
    matchLabels:
      app: scraper-runemetrics