apiVersion: apps/v1
kind: Deployment
metadata:
  name: bd-prd-api
spec:
  selector:
    matchLabels:
      app: bd-prd-api
  replicas: 1
  template:
    metadata:
      labels:
        app: bd-prd-api
    spec:
      containers:
        - name: bd-prd-api
          image: quay.io/bot_detector/bd-core-files:bed2adc
          ports:
            - containerPort: 4000
          env:
            - name: discord_sql_uri
              valueFrom:
                secretKeyRef:
                  name: core.files
                  key: discord_sql_uri
            - name: sql_uri
              valueFrom:
                secretKeyRef:
                  name: core.files
                  key: sql_uri
            - name: token
              valueFrom:
                secretKeyRef:
                  name: core.files
                  key: token
