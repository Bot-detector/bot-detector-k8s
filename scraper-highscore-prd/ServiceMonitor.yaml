apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scraper-hs
  namespace: bd-prd
spec:
  endpoints:
    - path: /
      port: metrics
  selector:
    matchLabels:
      app: scraper-highscore